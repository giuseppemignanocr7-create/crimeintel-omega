# ═══════════════════════════════════════════════
# CrimeIntel 7.0 Ω — Environment Configuration
# ═══════════════════════════════════════════════

# ── Database ──
DB_USER=crimeintel
DB_PASSWORD=crimeintel_secret          # ⚠️ CHANGE IN PRODUCTION
DB_NAME=crimeintel
DATABASE_URL=postgresql://crimeintel:crimeintel_secret@localhost:5432/crimeintel

# ── Authentication ──
JWT_SECRET=crimeintel-jwt-secret-CHANGE-ME   # ⚠️ CHANGE: use `openssl rand -hex 64` 
JWT_EXPIRES_IN=7d

# ── Backend ──
PORT=3000
NODE_ENV=development
STORAGE_ROOT=./uploads
CORS_ORIGIN=http://localhost:3001

# ── Dashboard ──
NEXT_PUBLIC_API_URL=http://localhost:3000/api/v1

# ── Mobile ──
EXPO_PUBLIC_API_URL=http://192.168.1.100:3000/api/v1

# ── AI Engine (future) ──
# AI_ENGINE_URL=http://localhost:8000
# OPENAI_API_KEY=sk-...
# ANTHROPIC_API_KEY=sk-ant-...
